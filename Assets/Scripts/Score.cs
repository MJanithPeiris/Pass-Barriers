using UnityEngine;
using UnityEngine.UI;

public class Score : MonoBehaviour
{
    public Transform coin;
    public Text scoreText;

    // Update is called once per frame
    void Update()
    {
        //scoreText.text = coin.position.z.ToString("0");
    }
}